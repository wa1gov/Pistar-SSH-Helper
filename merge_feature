##### Example how to merge feature changes into master
# git checkout feature
# git status
# git add Pistar-SSH-Helper.tgz psh-install.tgz
# git commit -m "Updated README.md"
# git push
# git status
# git checkout master
# git pull origin master
# git merge feature
# git push origin master
# git diff master feature
# git status

git status
echo "Merge feature into master? [y/N]"
read ans
if [ "$ans" = "y" ]
then
    git checkout master
    git pull origin master
    git merge feature
    git push origin master
fi
git status
